const data = {
  loading:null,
  status:null,
  message:null,
};
const agentReducer = (state=data,action) =>{
   switch(action.type) {
      default:
      return state;
   }
}
export default agentReducer;
